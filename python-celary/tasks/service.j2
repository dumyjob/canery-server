# service.j2
apiVersion: v1
kind: Service
metadata:
  name: {{ app_name }}-service
spec:
  type: {{ service_type }}
  selector:
    app: {{ app_name }}
  ports:
    - protocol: TCP
      port: {{ service_port }}
      targetPort: {{ container_port }}